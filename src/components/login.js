import React from 'react';
import { Button, Form } from 'react-bootstrap';

const Login = () => {
  return (
    <div>
      <Button>Login</Button>
    </div>
  );
};

export default Login;
